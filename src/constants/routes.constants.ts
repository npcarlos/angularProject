export enum PATHS {
  HOME = "/home",
  ARTIST = "/artist",
  MAIN = "/",
  EVENTS = "/events",
  NOT_FOUND = "*",
  SHOWS = "/shows",
  RIDERS = "/riders",
  SEARCH = "/search",
}

export enum SUB_PATHS {
  ELEMENT_DETAILS = "details",
}

export enum URL_PARAMETER_NAMES {
  ELEMENT_ID = "id",
}
